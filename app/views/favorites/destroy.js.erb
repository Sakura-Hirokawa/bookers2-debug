$("#favorite-btn_<%= @book.id %>").html("<%= j(render partial: 'favorites/favorite-btn', locals: {book: @book, favorite: @favorite}) %>");
$("#book_<%= @book.id %> .favorite-btn").html("<%= j(render 'favorites/favorite-btn', book: @book) %>");