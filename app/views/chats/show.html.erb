<% @room.user_rooms.each do |room| %>
  <% unless room.user.id == current_user.id %>
    <h4><%= room.user.name %>さん</h4>
  <% end %>
<% end %>

<div class="row">
  <div class="col-xs-8" id="chats">
    <%= render 'chats/show', chats: @chats %>
  </div>
</div>


<%= link_to "ユーザー一覧に戻る", users_path %><div id = "chats">
</div>
